import { Button } from "../ui/button";

export const SwapButton = () => {
  return (
    <Button className="h-10 gap-2 rounded-full pl-1.5 p-5 text-green-600 bg-green-800/20">
      Swap
    </Button>
  );
};
